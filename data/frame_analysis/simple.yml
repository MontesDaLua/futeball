# Configurações Técnicas e de IA
analysis:
  # yolov8n.pt
  # yolov8n.onnx
  # yolo11n.onnx
  # yolo11n.pt
  # yolo26n.onnx
  # yolo26n.pt
  # yolo26s.onnx
  # yolo26s.pt
  model_size: "yolov8n.pt" # one of the above
  min_confidence: 0.3
  sample_rate: 0.1  # Processar a cada 0.1 segundos
  device: "mps"     # 'cuda', 'cpu' ou 'mps'

# Classes do YOLO
target_classes:
  person: 0
  ball: 32
